stages:
  download:
    cmd: uv run python scripts/download_data.py
    outs:
    - data/raw:
        persist: true
  prepare:
    cmd: uv run python scripts/prepare_data.py
    deps:
    - configs/data.yaml
    - data/raw
    outs:
    - data/processed:
        persist: true
